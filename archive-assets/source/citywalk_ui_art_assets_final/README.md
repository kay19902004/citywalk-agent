# CityWalk UI Art Assets

透明资产已处理为真实 Alpha 透明，不是棋盘格假透明。

- `webp/`：推荐直接接入项目的 WebP 版本。
- `png/`：同名 PNG 版本。
- `manifest.json`：尺寸与透明信息。

`hero-wukang-bookstore` 与 `bg-city-sunny` 是不透明场景/背景资产；其余资产均含 Alpha 通道。
